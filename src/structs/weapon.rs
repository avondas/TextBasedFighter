pub struct Spell {
    pub name: String,
    pub damage: i32,
}

pub struct Sword {
    pub name: String,
    pub damage: i32,
}

pub struct Dagger {
    pub name: String,
    pub damage: i32,
}

pub struct Club {
    pub name: String,
    pub damage: i32,
}

pub struct Bow {
    pub name: String,
    pub damage: i32,
}

pub struct Axe {
    pub name: String,
    pub damage: i32,
}